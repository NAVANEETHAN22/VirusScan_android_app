<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background2">

    <!-- Wrap the buttons in a ScrollView -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/bottomLayout"
        android:layout_below="@+id/topLayout">

        <LinearLayout
            android:id="@+id/buttonsLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_centerInParent="true">

            <!-- Button to scan files -->
            <Button
                android:id="@+id/scanFilesButton"
                android:layout_width="347dp"
                android:layout_height="60dp"
                android:background="@drawable/rounded_button"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="290dp"
                android:text="Scan Files"
                android:textColor="@android:color/white"
                android:textSize="18sp" />

            <!-- Button to scan links -->
            <Button
                android:id="@+id/scanLinksButton"
                android:layout_width="346dp"
                android:layout_height="60dp"
                android:background="@drawable/rounded_button"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="20dp"
                android:text="Scan Links"
                android:textColor="@android:color/white"
                android:textSize="18sp" />

            <!-- Button for SQL Injection -->
            <Button
                android:id="@+id/sqlInjectionButton"
                android:layout_width="346dp"
                android:layout_height="60dp"
                android:background="@drawable/rounded_button"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="20dp"
                android:text="SQL Injection"
                android:textColor="@android:color/white"
                android:textSize="18sp" />

            <!-- Button for XSS -->
            <Button
                android:id="@+id/xssButton"
                android:layout_width="346dp"
                android:layout_height="60dp"
                android:background="@drawable/rounded_button"
                android:layout_gravity="center_horizontal"
                android:layout_marginTop="20dp"
                android:text="XSS"
                android:textColor="@android:color/white"
                android:textSize="18sp" />

        </LinearLayout>
    </ScrollView>

    <!-- Define top and bottom layouts -->
    <RelativeLayout
        android:id="@+id/topLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true">
        <!-- You can add any elements you want to remain at the top here -->
    </RelativeLayout>

    <RelativeLayout
        android:id="@+id/bottomLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true">
        <!-- You can add any elements you want to remain at the bottom here -->
    </RelativeLayout>

</RelativeLayout>
